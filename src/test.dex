def f (x, y : real, real; z : real) =
    let (a : real;) = x + x in
    let (b : real;) = (a * y) in
    let (;c : real) = b *. z in
    (b; c);

def main (;) =
    let (x : real;) = 2 in
    let (x2 : real;) = sin(x) in
    let (y : real;) = 3 in
    let (;z : real) = (0l : real) in
    f (x2, y; z)